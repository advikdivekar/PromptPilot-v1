Context = specificity. 
Without context, the tool writes good prompts for a generic developer.
With context, it writes the right prompt for me, in my codebase, right now.
The gap between those two is the entire value of this tool.